# Infiora API Contract

This package is the Wave 3 starting point for a shared contract layer.

Current scope:

- `generated/active-routes.json` is derived from the authoritative Node API route registry.
- Root command: `npm run contract:sync`

Future scope:

- OpenAPI export
- shared TypeScript DTOs
- frontend client generation
