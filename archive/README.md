# Archive

Wave 3 moved legacy snapshots and static copies here to keep the active workspace deterministic.

Contents:

- `legacy-app-main-before` - previous guest-app copy
- `legacy-static-frontend-frotnend` - static site/export snapshot
- `legacy-static-frontend-frbezanimacija` - static site/export snapshot
- `root-static-pages` - loose HTML pages that were previously at repository root

Archived content is not part of root scripts or deployment defaults.
